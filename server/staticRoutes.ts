import express from 'express';
import path from 'path';
import fs from 'fs';

const router = express.Router();

// Sitemap is now handled in server/index.ts with hardcoded content

export default router;